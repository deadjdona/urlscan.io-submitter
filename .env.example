# URLSCAN_API_KEY: urlscan.io API key for authenticated scan submissions.
# Required for submissions unless provided via CLI (-k) or config file.
URLSCAN_API_KEY="your-urlscan-api-key-here"

# PORT: Optional port for the Express backend server (default: 3001)
PORT=3001

